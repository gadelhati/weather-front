import { initialStructure } from "../structure/structure.initial";
import { Radar } from "./radar.interface";

export const initialRadar : Radar = {
    id: '',
    morse: '',
    structure: undefined,
}